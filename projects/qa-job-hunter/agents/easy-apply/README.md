# Agente Easy Apply

Implementación canónica en el hunter:

- Productivo: `npm run easy-apply` → `src/easy-apply.ts`
- Dry-run: `npm run easy-apply:dry-run` → `src/easy-apply-dry-run.ts`

Herramienta: Playwright + Chromium con `session/linkedin-session.json` (no es un producto aparte).
