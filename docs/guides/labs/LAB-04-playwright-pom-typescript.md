# LAB-04 — Playwright + POM (TypeScript) — próximo

**Estado:** guía preparada · practicar en chat con instructor  
**Stack:** Playwright · TypeScript · Page Object Model · OOP

---

## Objetivo

Automatizar UI (o API con `APIRequestContext`) usando **POM**, fixtures y buenas prácticas — alineado a `projects/api-testing` pero en **TS/JS**.

---

## Prerrequisitos (cuando arranques)

- LAB-00 OK
- Node 18+
- `npx playwright install` en el mini-proyecto que definamos

---

## Outline de pasos (instructor completará en sesión)

1. Init proyecto / carpeta `tests/playwright-ts/`
2. Base fixture + config (`playwright.config.ts`)
3. Primera **Page Object** (ej. dashboard Job Hunter o Swagger UI Node SUT)
4. Test smoke con AC ID en nombre (`@AC-xxx`)
5. Ejecutar headed vs headless
6. (Opcional) integrar en CI — ver LAB-02

---

## Para arrancar en chat

> **Lab Playwright POM, paso 1** — modo instructor, TypeScript.

**Chat:** mismo hilo si venís de LAB-03; chat nuevo si preferís sesión limpia solo UI.

---

## Referencia en repo

- Playwright C# (API): `projects/api-testing/tests/playwright-csharp/`
- Selenium Java UI: `projects/api-testing/tests/selenium-java/`

LAB-04 será el equivalente **TS + POM** para UI o API.
