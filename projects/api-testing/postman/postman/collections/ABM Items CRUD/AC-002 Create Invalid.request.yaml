$kind: http-request
url: "{{baseUrl}}/api/items"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: '{ "name": "" }'
scripts:
  - type: afterResponse
    code: pm.test('AC-002 status 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 2000
